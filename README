Work-in-progress for building Rebol 3 with Emscripten.

Build:

    make prep
    make CC="path/to/emcc -DNO_TTY_ATTRIBUTES" BIN_SUFFIX=".js" STRIP="echo"

Run:

    node r3.js --do 'print "hello, world!"'

Notes:

Currently developed & tested on Linux with Clang 3.2 and NodeJS 0.10.13.

The console is not yet working due to [a stdio
issue](http://stackoverflow.com/q/17753865/135724), so use "--do"
like above to test.
